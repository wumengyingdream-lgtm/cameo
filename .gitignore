# OS
.DS_Store

# Node / frontend
node_modules/
dist/
*.log
pnpm-debug.log*

# Rust / Tauri (if used)
target/
src-tauri/target/
src-tauri/gen/schemas/

# Managed-ffmpeg publish artifacts (publish_ffmpeg.sh): downloaded third-party
# binaries staged for upload + the generated manifest. Build artifacts, never committed.
dist-ffmpeg/
dist-ffmpeg-stage/

# Env / local secrets
.env
.env.*
!.env.example

# Claude Code dev tooling (machine-specific; includes vendored/symlinked skills).
# Kept local, out of the public repo.
.claude/
.agents/
/rclone.exe

# Local-only docs (developer thinking / process; NOT part of the open-source repo).
# Committed specs live at specs/ root (DESIGN.md, ...) and specs/tech_docs/.
specs/prd/
specs/research/

# Editors
.vscode/
.idea/
